# Agent Chrome profile -- contains session cookies, never commit
agent_chrome_data/

# Application run log — local only, not committed
agent_run_log.csv

# Local Windows desktop launchers — machine-specific, not committed
launch_portal.bat
launch_pooja.bat

# Secrets — never commit these
.env
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Data files (generated at runtime)
scan_log.txt

# OS
.DS_Store
Thumbs.db
